import { StyleSheet } from '@react-pdf/renderer'
//@TODO looks differently from layout styles... redo
const documentStyles = StyleSheet.create({})

const listStyles = StyleSheet.create({})

export {
  documentStyles,
  listStyles
}
